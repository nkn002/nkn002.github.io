source "https://rubygems.org"

# Specify the Jekyll version you want to use
# To upgrade, change the version below and run `bundle install`
gem "jekyll", "~> 4.3.2"

gem "wdm", "~> 0.1.0" if Gem.win_platform?

group :jekyll_plugins do
  gem "jekyll-feed"
  gem 'jekyll-sitemap'
end
